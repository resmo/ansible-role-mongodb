{% for user in mongo_users %}
db.createUser(
{
    user: "{{ user.name }}",
    pwd: "{{ user.pass }}",
    roles: [
      {% for role in roles -%}
      { role: {{ role.role }}, db: {{ role.db }} },
    ]
});
{% endfor %}
